version: "3.8"
name: redis
services:
  redis:
    image: redis/redis-stack-server:latest
    ports:
      - "6370:6379"
    healthcheck:
      test: [ "CMD", "redis-cli", "ping" ]
      interval: 10s
      timeout: 5s
      retries: 5

volumes:
  hatchet_lite_postgres_data:
  hatchet_lite_config: